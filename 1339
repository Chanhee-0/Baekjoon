#include<iostream>
#include<string>
#include<vector>
#include<array>
#include<algorithm>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int n, num;
	string s[10];
	vector<int> v;
	array<int, 10> arr;

	cin >> n;

	for (int i = 0; i < n; i++)
		cin >> s[i];
	for (int i = 9; i > 0; i++) { // 변수와 비교해서 같으면 i로 바꿈

	}
	for (int i = 0; i < n; i++) 
		cout << s[i] << endl;

	return 0;
}
/*
1339 단어 수학
자릿수를 이용하여 푼다.
aabb + babb // 사이즈와 앞자리가 같고 b가 하나가 많은 경우
1200 * a + 1022 * b
제일 큰 것을 9로 설정
9988 + 8988 = 18976 a9
8899 + 9899 = 18798 b9

*/
